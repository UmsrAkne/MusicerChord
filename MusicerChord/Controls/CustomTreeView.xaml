<UserControl
    x:Class="MusicerChord.Controls.CustomTreeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:behaviors="clr-namespace:MusicerChord.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:MusicerChord.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:DirectoryTreeViewModel}"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">

    <TreeView x:Name="DirectoryTree" ItemsSource="{Binding SoundContainers}">
        <TreeView.ItemContainerStyle>
            <Style BasedOn="{StaticResource {x:Type TreeViewItem}}" TargetType="TreeViewItem">
                <Setter Property="behaviors:TreeViewItemBehavior.ExpandedCommand" Value="{Binding LoadChildrenCommand}" />
            </Style>
        </TreeView.ItemContainerStyle>

        <TreeView.ItemTemplate>
            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                <ContentControl>
                    <b:Interaction.Triggers>
                        <b:EventTrigger EventName="MouseDoubleClick">
                            <b:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=TreeView}, Path=DataContext.OpenSoundContainerCommand}" />
                        </b:EventTrigger>
                    </b:Interaction.Triggers>

                    <TextBlock FontSize="{StaticResource CommonFontSize}" Text="{Binding Name}" />
                </ContentControl>
            </HierarchicalDataTemplate>

        </TreeView.ItemTemplate>
    </TreeView>

</UserControl>